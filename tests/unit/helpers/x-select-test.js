import { xSelect } from 'super-rentals/helpers/x-select';
import { module, test } from 'qunit';

module('Unit | Helper | x select');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = xSelect([42]);
  assert.ok(result);
});
